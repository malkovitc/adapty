import post from './post'
import category from './category'

// Export all schema types
export const schemaTypes = [post, category]

// Export individual schemas for type checking
export { post, category }
